__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
screenshots/
*.env
.env

# The journal web app is its own git repo, pushed separately to
# github.com/777-xevill/xetrade. Tracking it here would create a broken
# nested-repo reference.
journal-app/
